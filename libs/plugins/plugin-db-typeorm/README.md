# plugin-db-typeorm

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build plugin-db-typeorm` to build the library.

## Running unit tests

Run `nx test plugin-db-typeorm` to execute the unit tests via [Jest](https://jestjs.io).
